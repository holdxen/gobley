[package]
name = "gobley-fixture-trait-record-enum"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "lib"]
name = "trait_record_enum"

[dependencies]
uniffi = { workspace = true }
thiserror = "1"
